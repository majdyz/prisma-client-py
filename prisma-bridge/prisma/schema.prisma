
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./test.db"
}

model AgentBlock {
  id   String @id @default(cuid())
  name String
  type String
  
  @@map("AgentBlock")
}

model User {
  id    String @id @default(cuid())
  name  String
  email String @unique
  
  @@map("User")
}
